<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>ManagedCode.Playwright.Stealth</PackageId>
    <Title>ManagedCode.Playwright.Stealth</Title>
    <Description>Stealth evasion library for Microsoft.Playwright (.NET). Provides 31 conditional init-script patches that mask headless Chrome fingerprints while preserving native Chromium values when possible - navigator properties, WebGL, canvas, audio, CDP traces, automation markers and more. Validated against 9 public bot-detection sites.</Description>
    <PackageTags>playwright;stealth;bot-detection;headless;fingerprint;anti-detect;web-scraping;automation</PackageTags>
    <RootNamespace>ManagedCode.Playwright.Stealth</RootNamespace>
    <AssemblyName>ManagedCode.Playwright.Stealth</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Playwright" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources/js/*.js" />
  </ItemGroup>

</Project>
